prototype(Site:Subscribe) < prototype(Neos.Neos:Plugin)
prototype(Site:Subscribe) {
    package = 'DirectMail.Newsletter'
    controller = 'Recipient'
    action = 'subscribe'
    category = ${q(node).property('category')}
}